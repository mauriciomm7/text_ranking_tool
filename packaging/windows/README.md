

```
TextRankingTool_v1.0/
├── TextRankingTool.exe          ← Uncompressed standalone (larger but faster)
├── mock_installer_config.json   ← External configuration (customizable)
├── external_data/               ← Folder for user CSV files
├── README.txt                   ← Setup instructions
└── sample_data/                 ← Example CSV files (optional)
```